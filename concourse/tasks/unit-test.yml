---
platform: linux

inputs:
  - name: app-git
  - name: ci-config

caches:
  - path: cache/gomod
  - path: cache/gocache
  - path: cache/gobin

params:
  CI_CONFIG_DIR: ../ci-config

run:
  path: bash
  args:
    - concourse/scripts/unit-test.sh
  dir: app-git
